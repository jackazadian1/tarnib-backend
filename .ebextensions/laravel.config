files:
  "/tmp/envfile":
    mode: "000755"
    content: |
        APP_NAME=tarnib-backend
        APP_ENV=production
        APP_KEY=base64:sYyFAIYIEAHNwMI1xcbJOWabpJdePPIouwSVE1YoQDk=
        APP_DEBUG=false
        APP_URL=http://tarnib-backend-dev.us-east-1.elasticbeanstalk.com/

        LOG_CHANNEL=stack
        LOG_DEPRECATIONS_CHANNEL=null
        LOG_LEVEL=debug

        DB_CONNECTION=mysql
        DB_HOST=tarnib.ci4f84ihjrhz.us-east-1.rds.amazonaws.com
        DB_PORT=3306
        DB_DATABASE=tarnib
        DB_USERNAME=admin
        DB_PASSWORD=tarnib-rds-123

        BROADCAST_DRIVER=pusher
        CACHE_DRIVER=file
        FILESYSTEM_DISK=local
        QUEUE_CONNECTION=sync
        SESSION_DRIVER=file
        SESSION_LIFETIME=120

        MEMCACHED_HOST=127.0.0.1

        REDIS_HOST=127.0.0.1
        REDIS_PASSWORD=null
        REDIS_PORT=6379

        MAIL_MAILER=smtp
        MAIL_HOST=mailpit
        MAIL_PORT=1025
        MAIL_USERNAME=null
        MAIL_PASSWORD=null
        MAIL_ENCRYPTION=null
        MAIL_FROM_ADDRESS="hello@example.com"
        MAIL_FROM_NAME="${APP_NAME}"

        AWS_ACCESS_KEY_ID=
        AWS_SECRET_ACCESS_KEY=
        AWS_DEFAULT_REGION=us-east-1
        AWS_BUCKET=
        AWS_USE_PATH_STYLE_ENDPOINT=false

        PUSHER_APP_ID=livepost
        PUSHER_APP_KEY=livepost_key
        PUSHER_APP_SECRET=livepost_secret
        PUSHER_HOST=
        PUSHER_PORT=443
        PUSHER_SCHEME=https
        PUSHER_APP_CLUSTER=mt1

        VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
        VITE_PUSHER_HOST="${PUSHER_HOST}"
        VITE_PUSHER_PORT="${PUSHER_PORT}"
        VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
        VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
option_settings:
  aws:elasticbeanstalk:container:php:phpini:
    composer_options: "--no-dev"
container_commands:
  00_set_environment_variables:
    command: "source /tmp/envfile"
  01_update_permissions:
    command: "sudo chmod -R 775 /var/app/current/storage /var/app/current/bootstrap/cache && sudo chown -R webapp:webapp /var/app/current/storage /var/app/current/bootstrap/cache"
  01storage_link:
    command: "sudo php artisan storage:link"
  02migrate:
    command: "sudo php artisan migrate --force 2>&1 | sudo tee /var/log/eb-migrate.log"
    leader_only: true